# Non-secret per-environment config for docker-compose.deploy.yml.
# Selected via: docker compose -p tasktracker-dev --env-file .env.dev ...
FRONTEND_PORT=8081
POSTGRES_DB=taskdb_dev
POSTGRES_USER=taskuser
POSTGRES_PASSWORD=taskpass
